def reverse():
    length = len(data_list)
    s = length

    for x in range(0,(len(data_list)
        temp = data_list[x]
        data_list[x] = data_list[len(datalist)-1]

_list = [1,2,3,4,5]
print (reverse(_list)
