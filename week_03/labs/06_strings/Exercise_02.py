'''
Complete Exercise 8.3 (p.95) from the textbook.

Here's the code:

prefixes = 'JKLMBOPQ'
suffix = 'ack'

for letter in prefixes:
    print letter + suffix

'''

prefixes = 'JKLMBOPQ'
suffix = 'ack'

for letter in prefixes:

    if letter ==
    print(letter + suffix)
